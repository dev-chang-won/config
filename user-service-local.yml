spring:
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /h2-console
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:chang1:davinchiview
  jpa:
    hibernate:
      ddl-auto: update
  config:
    import: "optional:configserver:http://127.0.0.1:8888/"

# 환영 메시지를 위한 greeting
greeting:
  message: github Config [LOCAL] setting infomation!!!!!!!!!!!!!
